# Mock Service Worker configuration
# Set to "true" to use MSW mocks, "false" to use real backend
VITE_MOCK_ENABLED=true

# API URL (only used when MOCK_ENABLED=false in production builds)
# In development, Vite proxy handles /api -> http://localhost:8080
VITE_API_URL=